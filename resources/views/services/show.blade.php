@extends('layouts.app')
@section('content')
<section class="detail-hero"><div class="container"><a href="{{ route('services.index') }}" class="back-link">← All services</a><div class="detail-grid"><div><p class="eyebrow">{{ $service->category }} <span>✳</span></p><h1>{{ $service->title }}</h1><p class="detail-lede">{{ $service->excerpt }}</p><div class="detail-meta"><span>★★★★★ <small>{{ $service->review_count }} reviews</small></span><span>{{ $service->delivery_time }} delivery</span></div></div><div class="detail-image" style="background-image:url('{{ $service->image_url }}')"><span>{{ $service->icon }}</span></div></div></div></section>
<section class="detail-body"><div class="container detail-body-grid"><div><p class="eyebrow">The outcome</p><h2>Make your next move<br><em>feel inevitable.</em></h2><p class="body-copy">{{ $service->description }}</p></div><div class="feature-panel"><p class="eyebrow">What's included</p><ul>@foreach($service->features ?? [] as $feature)<li><span>↗</span>{{ $feature }}</li>@endforeach</ul><div class="price-row"><span>{{ $service->price_label }}</span><strong>${{ number_format($service->starting_price) }}<small> USD</small></strong></div><a href="#book" class="button button-dark full-width">Start a conversation <span>↗</span></a></div></div></section>
<section class="booking-section" id="book"><div class="container booking-grid"><div><p class="eyebrow">Let's talk</p><h2>Tell us about<br><em>your good thing.</em></h2><p class="booking-note">No pitch deck required. A little context is plenty. We’ll get back to you within two business days.</p></div><form class="booking-form" method="POST" action="{{ route('services.book', $service) }}">@csrf @if($errors->any())<div class="form-errors">Please check the highlighted fields and try again.</div>@endif<div class="form-row"><label>Your name<input name="name" value="{{ old('name') }}" required></label><label>Email address<input type="email" name="email" value="{{ old('email') }}" required></label></div><div class="form-row"><label>Company <span>(optional)</span><input name="company" value="{{ old('company') }}"></label><label>Working budget<select name="budget" required><option value="">Select one</option><option>$1k – $5k</option><option>$5k – $10k</option><option>$10k – $25k</option><option>$25k+</option></select></label></div><label>What are you hoping to make?<textarea name="project_details" rows="5" required placeholder="A few sentences about the challenge, opportunity, or idea...">{{ old('project_details') }}</textarea><button class="button button-accent" type="submit">Send inquiry <span>↗</span></button></form></div></section>
@endsection
